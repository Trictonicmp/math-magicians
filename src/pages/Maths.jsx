import Calculator from '../components/Calculator';

const Maths = () => (
  <section className="maths-section">
    <h2>
      { 'Let\'s do some math!!' }
    </h2>
    <Calculator />
  </section>
);

export default Maths;
